git_url(ENV["MATCH_GIT_URL"]) # private repo that stores the encrypted certs/profiles
storage_mode("git")
type("appstore")

# Every signable bundle id that ships inside the .ipa. Frameworks
# (CurrencyCore / Subscriptions) don't need a provisioning profile.
app_identifier([
  "gmg.CurrencyConverter",
  "gmg.CurrencyConverter.ListWidget",
  "gmg.CurrencyConverter.WidgetIntent",
])
